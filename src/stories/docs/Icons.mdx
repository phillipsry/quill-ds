import { Meta } from '@storybook/blocks'
import { IconGallery } from '../IconGallery'

<Meta title="Foundations / Icons" />

# Icons

Material Symbols (**Outlined**, weight 200) — the actual Google symbols, rendered via
`<Icon name="…" />`. Search the full library and click any icon to see its name, sizes,
and usage snippet. Icons load on demand, so browsing the whole set stays fast.

<IconGallery />
