import { Meta } from '@storybook/blocks'
import { IconGallery } from '../IconGallery'

<Meta title="Foundations / Icons" />

# Icons

Search the full library and click any icon to see its name, sizes, and usage snippet.
Icons load on demand, so browsing the whole set stays fast.

<IconGallery />
